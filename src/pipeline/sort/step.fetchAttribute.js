// For a given criterion and item, fetch attribute value (wiki-first, GPT normalize in accurate mode).
// Input: { item, criterion, mode }
// Output: { id, key, value, unit?, provenance: "wiki_infobox"|"wiki_table_or_ref"|"wiki_body"|"gpt_normalized"|"gpt_inferred" }


// Wikipedia adapter.
// Exports: search(query), listPages(topicOrUrl), fetchExtract(pageid), fetchInfobox(title), fetchPageviews(title)
// All functions should accept { signal } for abort/timeouts.


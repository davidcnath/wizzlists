// JSDoc/TS shapes used across the app (no implementation).
// type ListItem = { id: string; title: string; url?: string; blurb?: string; popularity?: number; }
// type SortSuggestion = { label: string; key: string; rationale?: string; expectedType: 'number'|'date'|'text' }
// SSE events: 'item' {ListItem}, 'rank' {order: string[]}, 'confidence' {value: number}, 'done' {count?}

